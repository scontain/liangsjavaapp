apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "random-chuck-norris.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "random-chuck-norris.name" . }}
    helm.sh/chart: {{ include "random-chuck-norris.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "random-chuck-norris.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
