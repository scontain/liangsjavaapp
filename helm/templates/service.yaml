apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-chuck-norris.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "random-chuck-norris.name" . }}
    helm.sh/chart: {{ include "random-chuck-norris.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "random-chuck-norris.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
